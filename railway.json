{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "nixpacks" },
  "deploy": {
    "startCommand": "soul-legacy serve --host 0.0.0.0 --port $PORT --cloud",
    "restartPolicyType": "on_failure"
  }
}
